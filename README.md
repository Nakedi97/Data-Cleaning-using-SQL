# Data-Cleaning-using-SQL
SQL pipeline that cleans raw layoffs data. Removes duplicates, standardizes text (companies, industries, countries), fixes data types (dates, decimals), and handles missing values. Transforms messy data into analysis-ready format using CTEs, window functions, and smart joins.
